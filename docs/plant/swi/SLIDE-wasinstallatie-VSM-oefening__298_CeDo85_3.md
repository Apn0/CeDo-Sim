# SLIDE — "De Wasinstallatie" (VSM training exercise cover)

- **Source file:** `298_CeDo85 (3).pdf` (1 page, photo of a printed flip-chart / training slide)
- **Type:** Lean training slide (flip-chart page), NOT a work instruction. Cover/prompt for a Value Stream Map exercise on the wash line.
- **Author/branding:** Cedo logo; `LEAN PRACTITIONER` badge (bottom-right); footer `Cedo Kunstofrecycling` (Cedo Plastics Recycling); year `2024`.

## Full verbatim content
- **Title:** `De Wasinstallatie` (The wash installation / washing line)
- **Body (large):**
  `ter plekke gaan waarnemen`
  `iedereen maakt een VSM`
  `van de flow`
  (Go observe on-site; everyone makes a VSM of the flow.)
- **Diagram:** 5 empty boxes connected left→right by arrows (□ → □ → □ → □ → □) — a blank process-step chain for trainees to fill in.
- **Caption:** `(value Stream Map)`
- **Footer band:** `Cedo Kunstofrecycling` · `2024`

## Answers to open questions
- **Q29 / wash line (line 6):** Confirms CeDo internally calls the wash line **"De Wasinstallatie"** and treats it as a 5-stage flow (the VSM has exactly 5 process boxes) — matches a typical wash-line sequence (e.g. infeed → pre-wash → friction/wash → rinse/separation → dewatering/drying). No stage labels are filled in on this blank exercise sheet, so the 5 specific stages are not named here. Useful confirmation that the wash installation is modeled as ~5 sequential stages.
- No SWI id, no step table, no measured values. This is a Lean-workshop artifact.

## Notes for sim
- Reinforces that the plant's Lean documentation program (Lean Practitioner, VSM exercises, the PROD-SWI sheets) is the origin of most of these scans.
- The wash installation = 5-step value stream (blank here); if in-sim you model the wash line as a linear 5-stage flow you match CeDo's own mental model.
